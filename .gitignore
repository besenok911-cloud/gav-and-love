# source media (not deployed)
photo/
# worker deps/state
worker/node_modules/
worker/.wrangler/
.DS_Store
Thumbs.db
desktop.ini
_crm_mock.html
.claude/
.wrangler/
worker/.dev.vars
# случайно попавшая сюда папка со сторонними файлами — не часть проекта
Archive/
# screenshots the design pass leaves behind
Claude outputs/

# памятка клиенту: содержит пароли, в репозиторий не попадает
handover/
